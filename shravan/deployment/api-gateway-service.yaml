apiVersion: v1
kind: Service

metadata:
  name: api-gateway-service
  namespace: mlops

spec:
  selector:
    app: api-gateway

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8004
      nodePort: 30084

  type: NodePort