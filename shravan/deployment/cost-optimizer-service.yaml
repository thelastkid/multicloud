apiVersion: v1
kind: Service

metadata:
  name: cost-optimizer-service
  namespace: mlops

spec:
  selector:
    app: cost-optimizer

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8003

  type: ClusterIP