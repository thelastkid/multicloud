apiVersion: v1
kind: Service

metadata:
  name: scheduler-service
  namespace: mlops

spec:
  selector:
    app: scheduler

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8001

  type: ClusterIP